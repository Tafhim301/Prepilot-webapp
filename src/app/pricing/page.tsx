import Pricing from "@/modules/pricing/Pricing";

export const metadata = {
  title: "Pricing | PrePilot",
  description:
    "Simple, transparent pricing for web agencies. Starter, Growth, and Enterprise plans with no hidden fees.",
};

export default function PricingPage() {
  return <Pricing />;
}
